# Gemini와 Spotify 키를 이 파일에서 정의한 형식으로 .env에 넣으면 됨.
GEMINI_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# 개인 계정으로 리프레시 토큰을 쓰고 싶으면 아래에 값을 넣으면 됨.
SPOTIFY_REFRESH_TOKEN=
# 리디렉트 URI를 커스텀으로 쓰고 싶으면 넣으면 됨.
SPOTIFY_REDIRECT_URI=
# 시장 코드를 바꾸고 싶으면 값을 바꾸면 됨.
SPOTIFY_MARKET=US
# 장르 시드를 직접 지정하고 싶으면 콤마로 구분해서 넣으면 됨.
# available-genre-seeds 엔드포인트에서 제공하는 값만 넣으면 됨.
SPOTIFY_DEFAULT_SEED_GENRES=pop,dance-pop
# 사용할 Gemini 모델을 바꾸고 싶으면 값을 바꾸면 됨.
GEMINI_MODEL=gemini-2.0-flash-exp
